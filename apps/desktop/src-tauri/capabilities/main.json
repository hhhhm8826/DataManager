{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal desktop permissions for the main DataManager window.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
